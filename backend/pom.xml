<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <version>3.3.4</version>
    <relativePath/>
  </parent>

  <groupId>br.com.milscale</groupId>
  <artifactId>milscale-backend</artifactId>
  <version>0.1.0</version>
  <name>milscale-backend</name>
  <description>
    MilScale - Sistema de Gestao de Escalas de Servico Militar.
    Backend monolito hexagonal (Ports and Adapters), conforme o documento
    "ATIVOS REUTILIZAVEIS" da equipe (Dev Reuso de Software, 6o periodo PUCPR).

    NOTA DE VERSAO: o documento original especifica Java 25 (LTS) + Spring Boot 4.1.
    Este projeto usa Java 21 (LTS) + Spring Boot 3.3 por serem as versoes estaveis e
    disponiveis no ambiente de desenvolvimento no momento da implementacao. A troca
    para Java 25 / Spring Boot 4.1 quando disponiveis nao exige mudanca de arquitetura.
  </description>

  <properties>
    <java.version>21</java.version>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <!-- A partir do JDK 23 o javac nao roda processadores de anotacao
         sozinho - sem isto o Lombok nao gera getters/builders e o build
         quebra. Nao muda nada no JDK 21 (imagem Docker). -->
    <maven.compiler.proc>full</maven.compiler.proc>
  </properties>

  <dependencies>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-web</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-mail</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-data-jpa</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-security</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-validation</artifactId>
    </dependency>

    <!-- Banco para desenvolvimento local: H2 em modo de compatibilidade MySQL.
         Permite rodar sem Docker/MySQL instalado, usando praticamente o mesmo SQL.
         O driver do MySQL real ja fica pronto para quando o docker-compose entrar. -->
    <dependency>
      <groupId>com.h2database</groupId>
      <artifactId>h2</artifactId>
      <scope>runtime</scope>
    </dependency>
    <dependency>
      <groupId>com.mysql</groupId>
      <artifactId>mysql-connector-j</artifactId>
      <scope>runtime</scope>
    </dependency>

    <dependency>
      <groupId>org.projectlombok</groupId>
      <artifactId>lombok</artifactId>
      <optional>true</optional>
    </dependency>

    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.springframework.security</groupId>
      <artifactId>spring-security-test</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
      </plugin>
    </plugins>
  </build>
</project>
